#ifndef ANIMATION_PREVIEW_HANDLE_H
#define ANIMATION_PREVIEW_HANDLE_H

#include "Animation.h"

class PreviewHandle {
    public:
       void PreviewAnimation(Animation& animation, int framerate);
};

#endif
